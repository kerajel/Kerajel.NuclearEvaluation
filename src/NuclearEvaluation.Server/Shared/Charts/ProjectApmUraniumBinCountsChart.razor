<RadzenChart>
    <RadzenChartTooltipOptions Shared="@true" />
    <RadzenLineSeries Smooth="@true" Data="@(_apmUraniumBinCounts[nameof(ApmBase.U234)])" CategoryProperty="Name" Title="@nameof(ApmBase.U234)" LineType="LineType.Dashed" ValueProperty="Count">
        <RadzenMarkers Visible="@true" MarkerType="MarkerType.Square" />
        <RadzenSeriesDataLabels Visible="@true" />
    </RadzenLineSeries>
    <RadzenLineSeries Smooth="@true" Data="@(_apmUraniumBinCounts[nameof(ApmBase.U235)])" CategoryProperty="Name" Title="@nameof(ApmBase.U235)" LineType="LineType.Dashed" ValueProperty="Count">
        <RadzenMarkers Visible="@true" MarkerType="MarkerType.Square" />
        <RadzenSeriesDataLabels Visible="@true" />
    </RadzenLineSeries>
    <RadzenLineSeries Smooth="@true" Data="@(_apmUraniumBinCounts[nameof(ApmBase.U236)])" CategoryProperty="Name" Title="@nameof(ApmBase.U236)" LineType="LineType.Dashed" ValueProperty="Count">
        <RadzenMarkers Visible="@true" MarkerType="MarkerType.Square" />
        <RadzenSeriesDataLabels Visible="@true" />
    </RadzenLineSeries>
    <RadzenLineSeries Smooth="@true" Data="@(_apmUraniumBinCounts[nameof(ApmBase.U238)])" CategoryProperty="Name" Title="@nameof(ApmBase.U238)" LineType="LineType.Dashed" ValueProperty="Count">
        <RadzenMarkers Visible="@true" MarkerType="MarkerType.Square" />
        <RadzenSeriesDataLabels Visible="@true" />
    </RadzenLineSeries>
</RadzenChart>