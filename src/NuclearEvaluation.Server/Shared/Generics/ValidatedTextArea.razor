@typeparam TModel

@inherits ValidatedTextControlBase<TModel, string>

<div class="validated-textarea-container" style="position: relative;">
    <RadzenTextArea id="@Id"
                    @ref="_textInputRef"
                    @bind-Value="_boundValue"
                    Style="@ComputedStyle"
                    Placeholder="@Placeholder"
                    @oninput="OnInput"
                    Visible="@Visible"
                    Rows="Rows"
                    @onblur="HandleOnBlur"
                    Name="@Name" />

    @if (Visible && _hasValidationErrors && !string.IsNullOrEmpty(_validationMessage))
    {
        <div class="validation-tooltip"
             style="--tooltip-offset-x:@TooltipOffsetXpx; --tooltip-offset-y:@TooltipOffsetYpx;">
            @(_validationMessage)
        </div>
    }
</div>

